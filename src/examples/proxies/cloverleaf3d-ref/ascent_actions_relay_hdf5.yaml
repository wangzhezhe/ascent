#An example of using a relay extract to save a subset of the data.

- 
  action: "add_extracts"
  extracts: 
    e1: 
      type: "relay"
      params: 
        path: "/home/zw/cworkspace/src/performanceStudy/src/ascent/build-debug/examples/proxies/cloverleaf3d-ref/testoutput"
        protocol: "blueprint/mesh/hdf5"
- 
  action: "execute"
